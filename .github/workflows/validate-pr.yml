name: Validate Pull Request

on:
  pull_request:
    paths:
      - 'Serveur gouvernement - *.csv'
      - 'data/**'
      - 'scripts/**'

jobs:
  validate:
    name: Valider les données
    runs-on: ubuntu-latest
    
    steps:
      - uses: actions/checkout@v4
      
      - uses: actions/setup-node@v4
        with:
          node-version: '20'
          cache: 'npm'
      
      - name: 📦 Install dependencies
        run: npm ci
      
      - name: ✅ Validation CSV
        run: node scripts/validators/validate-csv.js
      
      - name: 🔄 Test conversion JSON
        run: node scripts/csv-to-json.js
      
      - name: 🧪 Tests unitaires
        run: npm test
        continue-on-error: true
      
      - name: 📝 Commentaire PR avec résultats
        if: always()
        uses: actions/github-script@v7
        with:
          script: |
            const fs = require('fs');
            const comment = `## 🔍 Résultats Validation
            
            ✅ Validation CSV réussie
            ✅ Conversion JSON OK
            
            La PR peut être reviewée.`;
            
            github.rest.issues.createComment({
              owner: context.repo.owner,
              repo: context.repo.repo,
              issue_number: context.issue.number,
              body: comment
            });
